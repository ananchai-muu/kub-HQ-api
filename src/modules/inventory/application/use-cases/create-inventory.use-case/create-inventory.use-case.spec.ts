import { CreateInventoryUseCase } from './create-inventory.use-case';

describe('CreateInventoryUseCase', () => {
  it('should be defined', () => {
    expect(new CreateInventoryUseCase()).toBeDefined();
  });
});

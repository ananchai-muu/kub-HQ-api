import { InventoryController } from './inventory.controller';

describe('InventoryController', () => {
  it('should be defined', () => {
    expect(new InventoryController()).toBeDefined();
  });
});

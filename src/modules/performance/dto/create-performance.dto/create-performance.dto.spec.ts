import { CreatePerformanceDto } from './create-performance.dto';

describe('CreatePerformanceDto', () => {
  it('should be defined', () => {
    expect(new CreatePerformanceDto()).toBeDefined();
  });
});

import { PerformanceController } from './performance.controller';

describe('PerformanceController', () => {
  it('should be defined', () => {
    expect(new PerformanceController()).toBeDefined();
  });
});

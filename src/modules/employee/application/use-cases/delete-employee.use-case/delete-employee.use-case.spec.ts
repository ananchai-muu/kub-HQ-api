import { DeleteEmployeeUseCase } from './delete-employee.use-case';

describe('DeleteEmployeeUseCase', () => {
  it('should be defined', () => {
    expect(new DeleteEmployeeUseCase()).toBeDefined();
  });
});

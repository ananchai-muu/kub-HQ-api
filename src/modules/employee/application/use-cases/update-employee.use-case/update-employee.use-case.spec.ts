import { UpdateEmployeeUseCase } from './update-employee.use-case';

describe('UpdateEmployeeUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateEmployeeUseCase()).toBeDefined();
  });
});

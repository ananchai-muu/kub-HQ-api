import { GetEmployeeListUseCase } from './get-employee-list.use-case';

describe('GetEmployeeListUseCase', () => {
  it('should be defined', () => {
    expect(new GetEmployeeListUseCase()).toBeDefined();
  });
});

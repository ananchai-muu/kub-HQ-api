import { BranchRepositoryInterface } from './branch.repository.interface';

describe('BranchRepositoryInterface', () => {
  it('should be defined', () => {
    expect(new BranchRepositoryInterface()).toBeDefined();
  });
});

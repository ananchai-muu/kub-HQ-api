import { CreateMarketingUseCase } from './create-marketing.use-case';

describe('CreateMarketingUseCase', () => {
  it('should be defined', () => {
    expect(new CreateMarketingUseCase()).toBeDefined();
  });
});

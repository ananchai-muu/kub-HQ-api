import { MarketingController } from './marketing.controller';

describe('MarketingController', () => {
  it('should be defined', () => {
    expect(new MarketingController()).toBeDefined();
  });
});

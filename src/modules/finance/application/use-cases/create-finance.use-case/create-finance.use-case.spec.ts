import { CreateFinanceUseCase } from './create-finance.use-case';

describe('CreateFinanceUseCase', () => {
  it('should be defined', () => {
    expect(new CreateFinanceUseCase()).toBeDefined();
  });
});
